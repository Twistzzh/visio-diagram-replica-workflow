interface:
  display_name: "Visio Diagram Replica Workflow"
  short_description: "Replicate diagrams as editable Visio with preview QA."
  brand_color: "#00A6C8"
  default_prompt: "Use $visio-diagram-replica-workflow to replicate this reference diagram as an editable Visio file and export a preview."

policy:
  allow_implicit_invocation: true
